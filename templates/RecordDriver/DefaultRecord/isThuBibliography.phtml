<?php
    $OpenAccess = is_callable([$this->driver, 'isThuBibliography']) ? $this->driver->isThuBibliography() : false;
?>
<? if ($OpenAccess) :?>
<img alt="<?=$this->translate('Th_Biblio')?>" src="<?=$this->imageLink('ThüringenBib_Icon.png')?>" class="thbib-img" />
<? endif;?>