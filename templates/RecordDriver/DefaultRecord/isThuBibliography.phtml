<?php
    $OpenAccess = is_callable([$this->driver, 'isThuBibliography']) ? $this->driver->isThuBibliography() : false;
?>
<? if ($OpenAccess) :?>
<span class="label th-bibliography" title="<?=$this->translate('Th_Biblio')?>"><?=$this->translate('Th_Biblio')?></span>
<? endif;?>